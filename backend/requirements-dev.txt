# Test-only dependencies (kept out of the runtime image).
# Install alongside the runtime deps to run the suite:
#   pip install -r requirements.txt -r requirements-dev.txt && pytest
pytest==8.3.3
pytest-asyncio==0.24.0
