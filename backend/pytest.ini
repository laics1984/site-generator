[pytest]
testpaths = tests
# auto mode: bare `async def test_*` run under pytest-asyncio without a per-test
# marker. Matches the suite's existing style (no @pytest.mark.asyncio anywhere).
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
